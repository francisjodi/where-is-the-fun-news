import {fetchFavorites} from "../favoritesService";

export default function SavedArticles() {

  // const articles = await fetchFavorites();
  // console.log(await articles)

  return (
    <div>
      <div>
        <p>SAVED ARTICLES HERE</p>
      </div>
    </div>
  )
}